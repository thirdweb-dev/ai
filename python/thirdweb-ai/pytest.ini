[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Add markers if needed
markers =
    unit: Unit tests
    integration: Integration tests

# Configure verbosity
addopts = -v